"""Adapters connect a live factory to the measurement interfaces.

`skeleton.py` is a documented, runnable-shaped template: subclass it, fill the
methods that pull from your factory's real traffic, and the same measurement
tracks that run on the mock run against your factory unchanged.
"""
